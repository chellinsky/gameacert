<% if user_signed_in? %>
	<h1>Editing game</h1>

	<%= render 'form' %>

	<%= link_to 'Show', @game %> |
	<%= link_to 'Back', games_path %>
<% else %>
	<p>You must be logged in as an administrator to make a new game</p>
<% end %>